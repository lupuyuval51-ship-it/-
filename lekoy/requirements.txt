# LEKOY RV5 — CPU install, which is what reports/system_report.md describes.
#
#   python -m venv .venv && source .venv/bin/activate
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
#
# Install torch first and from the CPU index: the default index pulls the CUDA
# runtime, which is roughly 3 GB of libraries this host cannot use.
#
# For a GPU host, see requirements-gpu.txt.

torch>=2.1
transformers>=4.44
accelerate>=0.30
peft>=0.11
datasets>=2.19
tokenizers>=0.19
safetensors>=0.4
sentencepiece>=0.2
huggingface_hub>=0.23
pyarrow>=14.0
numpy>=1.24
pyyaml>=6.0

# Serving: the OpenAI-compatible API and the web chat.
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

# Evaluation and reporting.
sacrebleu>=2.4
scikit-learn>=1.4
tensorboard>=2.16

# Tests.
pytest>=8.0
httpx>=0.27
